# Build Planning

This directory documents build strategies only. Android source trees, `out/`
directories, compiler caches, firmware packages, and generated images must stay
outside the repository.

